version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
      - SUPABASE_URL=https://your-project.supabase.co
      - SUPABASE_KEY=your-service-role-key
